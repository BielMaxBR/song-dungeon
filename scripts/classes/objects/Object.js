export default class GameObject {
    constructor(position) {
        // this.renderType = renderType
        this.position = position
    }
    render(mapManager) {
    }
    update(delta) {
    }
}
